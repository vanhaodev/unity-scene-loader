Add the following scenes in Build Settings:

SceneLoader.Scene1 → index 0
SceneLoader.Scene2 → index 1

Start play at SceneLoader.Scene1

This ensures the loader system can find scene indices correctly.